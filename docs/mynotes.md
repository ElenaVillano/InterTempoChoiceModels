


## My notes on the model


Currently, the tradeoff model (Scholten & Read, 2010) offers the
broadest coverage of reported preference patterns, but its scope has
come at the cost of substantial complexity: There are now various
instantiations of the model dealing with partially overlapping
universes of choice options and preference patterns (Read &
Scholten, 2012; Scholten & Read, 2010, 2013; Scholten et al., 2014,
2016). 


The unified tradeoff model, that is simpler, yet more comprehensive, than all currently available versions of the tradeoff model taken together. An element
that the unified tradeoff model and its predecessors share with most
other models of intertemporal choice is positive time preference
(Fisher, 1930), meaning a preference for obtaining positive
outcomes as soon as possible, and incurring negative outcomes
as late as possible. The unified tradeoff model, however, introduces
a new element, time bias, that enables it to accommodate an
extraordinarily broad range of preference patterns, and to generate
new predictions that contradict all other models of intertemporal
choice, attribute- and alternative-based. We put these distinctive
predictions to the test, and see them confirmed.

## Time bias

Concierto de tu banda favorita será en un año. 

### Sesgo temporal

Primero piensas: Tengo que esperar (esto no toma en cuenta cuánto debes de esperar).

- Positivo: Preferencia por buenas cosas más pronto, y malas después
    - Por lo menos no es ahora (para cosas malas)
- Negativo: Preferencia por buenas cosas después y malas cosas más pronto.
    - Terminemos con esto ahora (para cosas malas)
 
400 pesos ahora o x después 

El punto de indiferencia es cuando x vale lo mismo que 400, pero demorado.

Preferencia temporal positiva significa que recibir 400 ahora tiene una ventaja temporal, cuya magnitud aumenta en función de cuánto más tarde se vayan a recibir los $x

La compensación crece con el tiempo de espera. Esto es lo que tradicionalmente se llama "descontar el futuro": cada unidad extra de tiempo añade descontento, así que cada unidad extra de tiempo requiere más dinero para compensarse.

El sesgo temporal positivo significa que recibir 400 ahora tiene una ventaja temporal adicional, cuya magnitud no depende de cuánto más tarde se vayan a recibir los x. En el punto de indiferencia, x será igual a 400 más el efecto variable de la preferencia temporal positiva más el efecto fijo del sesgo temporal positivo.

Matemáticamente, este componente está escalado por T=w(tL)−w(tS)T = w(t_L) - w(t_S)
T=w(tL​)−w(tS​) — la diferencia temporal efectiva — multiplicado por κ\kappa
κ (la fuerza de la preferencia temporal). Si tLt_L
tL​ crece, TT
T crece, y la compensación necesaria crece.


Esta es la parte novedosa del UTM, y es más sutil. La idea es que además de la impaciencia que crece con el tiempo, hay una especie de "peaje fijo" psicológico por el simple hecho de tener que esperar algo, sin importar cuánto. Es como si la persona dijera: "Solo por hacerme esperar — no importa si es un día o un año — quiero $5 extra. Y encima de eso, quiero más cuanto más larga sea la espera."

### Preferencia temporal
Después piensas: Tengo que espera todo un año, después ya es cuando el tiempo se toma en cuenta. 



Time 

Value function: 

1. Reference dependence: The outcomes are evaluatare as positive deviations from reference point.
2. Diminishigs absolute sensitivity: As outcome magnitude increases by constant absolute amounts, positive outcome value increases, and negative outcome value decreases, decreasing absolute amouts.
3. Augmenting proportional sensitivity: (Absolute magnitude effect): Conforme la magnitud de la recompensa incremente por una constante proporiconal, el valor de la recompensa incrementa, incrmentando la magnitud absoluta. Si v(200)-v(100)> que v(20)- v(10). Si incrementas proporcionamente una cantidad entonces el valor de la recompensa será mayor, pero más en cantidades grandes.
4. Loss aversion: Losses are twice as impacttul as gains, and large amounts are half as impactful as small amounts.

This applies for the time-weighing function. 

The difference between time preference and time bias is that the former operate as a multiplicative constant that scales with time, while the latter operates as an additive constant that scales with time, while the latter operates as an additive constant that does not scale with time. 

Time bias:

$$ $$


menos aversos al retraso sobre el intervalo no dividido que sobre el dividido (subaditividad).

El modelo de tradeoff unificado atribuye esto al sesgo temporal positivo en ganancias: opera en la misma dirección que la preferencia temporal positiva, hacia la aversión al retraso, poniendo una penalización constante sobre la ganancia posterior en cada intervalo considerado, lo que significa una vez sobre el intervalo no dividido, pero dos veces sobre los dos subintervalos adyacentes, de modo que la aversión al retraso será más débil sobre el intervalo no dividido que sobre el dividido.


## Change in the article:

### Unified Tradeoff Model

Is the simpler, yet more comprehensive model than all currently available versions of the tradeoff model taken together (Scholten et al., 2024). The unified tradeoff model, introduces *time bias* that enables it to accommodate a broader range of preference patterns. 



#### Efective outcome difference:

$X=v(x_L)-v(x_L)$

#### Effective time difference 

$T=w(t_L) - w(t_s)$

#### Indifference poin

$kT=X$

where $k>0$ is the strenght of the time preference 

tiene $\sigma$ pero es igual a 1 si son recompensas. 
